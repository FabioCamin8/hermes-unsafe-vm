# Private operator file for converting one already-created disposable candidate.
TEMPLATE_VMID=
TEMPLATE_NAME=
SSH_USER=hermes
SSH_HOST=
SSH_KEY_FILE=
GUEST_REPO=/root/hermes-unsafe-vm
# Keep this outside the repository; it contains source identity fingerprints.
SOURCE_EVIDENCE_FILE=/tmp/hermes-unsafe-vm-template-source
